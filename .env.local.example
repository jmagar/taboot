# .env.local - Local development overrides
#
# This file shows how to override service URLs when running services locally
# instead of in Docker containers. Copy to .env.local and uncomment the services
# you're running locally.
#
# The config loader loads .env first, then .env.local to override for local dev.
# See packages/common/config/__init__.py:ensure_env_loaded()

# ========== Service URLs (override container names with localhost) ==========

# When running services locally, override container hostnames:
# FIRECRAWL_API_URL="http://localhost:3002"
# REDIS_URL="redis://localhost:6379"
# QDRANT_URL="http://localhost:6333"
# NEO4J_URI="bolt://localhost:7687"
# TEI_EMBEDDING_URL="http://localhost:8080"
# RERANKER_URL="http://localhost:8000"
# OLLAMA_URL="http://localhost:11434"
# PLAYWRIGHT_MICROSERVICE_URL="http://localhost:3000/scrape"

# ========== Database Connection (override container hostname) ==========

# When running PostgreSQL locally:
# POSTGRES_HOST="localhost"

# Keep credentials the same as .env
# POSTGRES_USER="taboot"
# POSTGRES_PASSWORD="your-password-from-env"
# POSTGRES_DB="taboot"
# POSTGRES_PORT="5432"
