#!/usr/bin/env bash
# Taboot environment setup - uses .env file directly

# Source local overrides first
if [[ -f .envrc.local ]]; then
    source .envrc.local
else
    # Fallback: load .env directly
    if [[ -f .env ]]; then
        set -a
        source .env
        set +a
    fi
fi
