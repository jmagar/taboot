{
  "name": "@taboot/user-lifecycle",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist .turbo node_modules",
    "dev": "tsc --build --watch",
    "lint": "eslint . --max-warnings 0",
    "test": "NODE_OPTIONS='--max-old-space-size=2048' jest --forceExit"
  },
  "prettier": "@taboot/prettier-config",
  "dependencies": {
    "@taboot/db": "workspace:*",
    "@taboot/logger": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "catalog:dev",
    "@taboot/eslint-config": "workspace:^",
    "@taboot/jest-presets": "workspace:*",
    "@taboot/prettier-config": "workspace:*",
    "@taboot/typescript-config": "workspace:*",
    "jest": "^30.1.3",
    "ts-jest": "^29.2.5",
    "typescript": "catalog:dev"
  }
}
